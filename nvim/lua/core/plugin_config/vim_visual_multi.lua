-- Plugin: vim-visual-multi

-- Settings
-- Set the multi-cursor highlight theme (non-default behavior).
vim.cmd("let g:VM_theme = 'iceblue'")
-- Set the color for matches highlighted by multi-cursor (non-default behavior).
vim.cmd("let g:VM_highlight_matches = 'red'")
